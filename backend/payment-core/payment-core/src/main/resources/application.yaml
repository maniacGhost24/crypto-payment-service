spring:
  datasource:
    url: jdbc:postgresql://localhost:5432/cryptopayments
    username: admin
    password: admin123
    hikari:
      connection-init-sql: SET TIME ZONE 'UTC'

  jpa:
    hibernate:
      ddl-auto: update

    properties:
      hibernate:
        jdbc:
          time_zone: UTC

    show-sql: true

server:
  port: 8080